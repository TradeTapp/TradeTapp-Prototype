(function () {
  'use strict';
  angular.module('tradeTapp.newsubform').factory('newsubform', newsubform);

  newsubform.$inject = ['$http','$q'];

  function newsubform ($http,$q) {
    var service = {
    };

    return service;
  }

})();